# ADR-373：公网默认干预方案对齐完整预热缓存

状态：Implemented and publicly verified
日期：2026-08-14
范围：M35 Presentation 新建实验、M34 Luna 缓存、公网部署

> 2026-08-14 质量审计发现，本 ADR 记录的早期 220 次调用/零 fallback 只能证明旧校验器接受输出，不能证明互动记录完整。活动缓存已按 `ADR-374-m354-deepseek-cache-quality-gate.md` 隔离并以 v3 质量门禁重建；默认参数不变，最新公网证据以 ADR-374 为准。

## 决策

- 原始方案和“2025 年政策参考基线”保持西部/中部/东部 `95%/90%/85%`。
- 公网 Presentation 新建实验的干预方案初始预设改为 `96%/93%/82%`。
- 用户仍可修改三档比例；非预热组合继续走 `Cache-first → DeepSeek live 补齐 → 显式 fallback`。
- `96%/93%/82%` 只表示比赛现场的预热演示路径，不代表现实最优、推荐比例或现实政策建议。

## 完成门禁

1. 使用无事件的纯政策对比完整运行 Q1–Q4，补齐所有合法决策缓存。
2. 以相同语义重新创建独立实验并完整运行 Q1–Q4。
3. 第二次运行缓存文件数不增加，全部主体调用 `fallback_used=false`。
4. 公网启动页显示原始方案 `95/90/85`、干预方案 `96/93/82`，健康检查仍为 `run_mode=cache`、`cache_miss_mode=live`。

## DeepSeek 补齐修复

修复前 `authorized-inbox-v1` 只提供消息 ID 和公开摘要，却要求模型满足完整领域校验，导致模型看不到官方 31 省集合、年度剩余资源、待回应会话内容和上一行动。第一次输出失败后的修复请求也未回传上一条 JSON，无法真正定向修复。

`m34-live-authorized-context-v2` 新增合法输出所需的授权约束、可见消息、待回应会话和上一行动；仍不向模型披露确定性候选行动或最终环境结果。修复轮把上一条模型 JSON 作为 assistant 消息回传，并使用分阶段、脱敏错误类型。合法旧缓存继续校验读取并自动提升为 v2 键。

## 公网验证证据

- 修复前受控样本：200 次调用中 108 次 fallback。
- 修复后全新参数 `96%/92%/83%` 的 Q1：82 次调用、8 次 fallback；用于定位剩余领域约束。
- 最终修复后全新参数 `96%/93%/82%`：Q1 82/82、Q2 累计 174/174、Q3 累计 220/220，完整年度实验 `exp_m34_8af7376aa96c` 最终 266 次调用零 fallback。
- 干净预热实验 `exp_m34_61e2da36eeaa`：Q1–Q4 共 220 次调用，零 fallback。
- 热缓存复验 `exp_m34_2e560e354667`：Q1–Q4 共 220 次调用，零 fallback；缓存文件数保持 831→831。
