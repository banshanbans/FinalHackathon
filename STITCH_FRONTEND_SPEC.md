# PolicyScope V3.2 正式前端规范

> 产品：PolicyScope / 政策涟漪
> 规范版本：V3.2 M34（季度事件驱动升级实施中）
> 更新日期：2026-08-13
> 主验收画布：1536 × 1024、1440 × 900、1280 宽
> 当前门禁：以当前 React 实际运行页为视觉基线；Stitch 降为历史参考

---

## 0. V3.2 旅程、导航与路由

顶层任务顺序固定为政策输入、中央解读、实验设计、基线确认、推演运行、结果复盘。一级导航为“新建实验 / 实验运行 / 参与主体 / 结果与对比 / 方法与数据”，不固定任何单省导航。

正式路由：

- `/experiments/new`
- `/experiments/:id/setup?step=interpretation|design|baseline`
- `/experiments/:id/live`
- `/experiments/:id/participants`
- `/experiments/:id/provinces/:provinceCode`
- `/experiments/:id/compare`
- `/experiments/:id/methods`

Live 使用左侧实验时间轴、中间离线中国地图和右侧关键行动流；Participants 集中浏览 31 省与 10 车企；Compare 首屏先回答 Gap 方向、省份与企业变化、财政代价、事件稳健性和三条机制链。Checkpoint、哈希、Replay、Audit 和原始 JSON 只进入 Methods。

页面必须使用严格 `productLabels`；未知值统一显示“未知状态”并在开发环境告警。主页面不得出现英文枚举、snake_case、`Y2_Q2`、Checkpoint ID 或 WorldState。M29 基线统一显示“可信数据”，来源与推算方法可下钻 Evidence，不在主页面重复展示多套质量标签。

## 1. 规范目的

本文件把 V3.0“中央—省级—真实头部车企”新能源汽车政策机制转化为正式 React 产品的页面、地图、交互、状态、文案和验收契约。

### 1.1 M32 产品信息层级

- 新建实验、设置、运行、参与主体、省份详情和结果对比只呈现用户可操作的方案、最终行动与结果；不得展示 Agent、版本、轮次、资源上限、阈值、机会成本、追溯、哈希或缓存等工程字段。
- 省份详情固定为“政策配置、企业互动、竞争与协同、推演影响”四张结果卡；不展示画像、初始方案、信号、原因链、取舍或 Delta 技术记录。
- 车企侧栏只展示行动概览、合作结果和产能意向。方法与数据页是版本、公式、Replay、Audit 和缓存的唯一审计入口。
- 全站主产品使用“省份”“车企”“政策解读”等业务称谓；仅方法页可出现 Agent 等技术术语。模拟边界以一条紧凑标签表达，不重复解释内部实现。

V3.0 前端必须帮助中央层面用户完成：

1. 设定并批准西部/中部/东部中央承担比例。
2. 在中国地图上观察 31 省如何配置消费端、固定成本、可变成本补贴。
3. 查看 10 家真实头部车企模拟 Agent 的全国销售投入和产能布局选择。
4. 观察 WTP、电池供应链、财政空间与 Peer Policy 的传播机制。
5. 在首年年末审批、修改或拒绝中央 Agent 的一次干预建议。
6. 比较同一首年 Checkpoint 派生的次年原始方案/干预方案及 `ΔGap`。

中国地图是主分析画布，六项中央指标是决策摘要，不再作为挤压地图的页面主体。视觉质量、产品真实性、数据边界和审批可信度同等重要。

---

## 2. 适用范围与实现状态

### 2.0 V3 契约快照

- 默认政策：西部 95%、中部 90%、东部 85%。
- 阶段：`SETUP → Y1_Q1 → Y1_Q2 → Y1_Q3 → Y1_Q4 → YEAR1_REVIEW → Y2_Q1 → Y2_Q2 → Y2_Q3 → Y2_Q4 → COMPLETE`。
- 十家车企：比亚迪、吉利、长安、上汽通用五菱、蔚来、奇瑞、零跑、赛力斯、小米汽车、理想汽车。
- 六项指标：区域发展差距、中央财政负担、地方财政压力、新能源汽车需求、新增投资集中度、产业集聚度。
- 版本：`policy-v3`、`province-profile-v4`、`province-persona-v2`、`province-action-v4`、`province-feedback-v4`、`automaker-profile-v1`、`automaker-action-v1`、`world-state-v4`、`comparison-v4`、`event-v4`。
- 路由：`/experiments/new`、`/experiments/:id/live`、`/experiments/:id/provinces/:provinceCode`、`/experiments/:id/intervention`、`/experiments/:id/compare`。

### 2.1 P0

- 桌面 Web，1536×1024 与 1440×900 形成完整地图工作台。
- 1280 宽允许纵向重排，但不得出现页面级横向滚动。
- 五个正式路由、车企侧栏、Evidence 抽屉和分支深链。
- 消费 V3 目标 DTO：`policy-v3`、`province-profile-v4`、`province-persona-v2`、`province-action-v4`、`province-feedback-v4`、`automaker-profile-v1`、`automaker-action-v1`、`world-state-v4`、`comparison-v4`、`event-v4`。
- 三档比例输入、两次审批、首年季度、次年同源 A/B。
- 31 省离线矢量地图和九类业务图层。
- 10 家车企模拟活动及明确真实数据/模拟行为边界。
- Loading、Empty、Running、Awaiting Approval、Approved、Completed、Fallback、Failed、Reconnecting 状态。

### 2.2 非 P0

- 移动端完整适配。
- 3D 地球、复杂飞线和装饰性动效。
- 独立企业管理页、Replay 管理页或自定义仪表盘。
- 单车型、单城市、单工厂或真实招商详情页。
- 企业 Logo、代言素材或品牌营销视觉。
- 自动参数寻优和批量场景矩阵。

### 2.3 当前事实

- V2 历史前端和 Design QA 已完成。
- V2.1 五路由代码已实现但最终验证未完成。
- V3.0 五路由、地图主画布、车企侧栏、Evidence、Fallback/重连状态和同源 Compare 已实现并通过三画布 QA。

---

## 3. 设计来源与优先级

### 3.1 来源用途

| 来源 | V3 用途 | 不得复用 |
|---|---|---|
| `stitch_policyscope/_1/screen.png` | 中央政策编辑、双栏结构和审批 CTA 节奏 | 制造业设备更新字段与 T0 文案 |
| `stitch_policyscope/_2/screen.png` | 地图、行动流和时间线相对位置 | KPI 主导布局、旧指标和 T2 阶段 |
| `stitch_policyscope/_3/screen.png` | 省级详情的高密度卡片和三栏节奏 | 六类企业卡、现实金额和伪置信度 |
| `stitch_policyscope/_4/screen.png` | “证据 → 建议 → 审批”三栏结构 | 旧工具 Diff 与 T3 文案 |
| `stitch_policyscope/a_b/screen.png` | 双地图、同源说明和迁移区布局 | 空白地图、现实金额和“优化方案” |
| `stitch_policyscope/policyscope/DESIGN.md` | 设计令牌、字体、颜色和制度工作台语言 | 旧制造业业务语义 |
| Stitch `code.html` | 尺寸和布局测量 | 静态数据、CDN、假路由和正式运行代码 |

现有高密度全国态势原型只保留壳层密度、卡片节奏和信息组织参考。V3 正式母版改为“地图优先政策模拟画布”。

### 3.2 冲突优先级

1. V3 PRD 与获批后的 V3 Schema/API DTO。
2. 本文件的页面、地图和状态契约。
3. `DESIGN.md` 的视觉令牌。
4. Stitch `screen.png` 的视觉节奏。
5. Stitch `code.html` 的测量数据。
6. V2/V2.1 现有前端代码。

---

## 4. 品牌与视觉语言

### 4.1 品牌与身份

全站只使用：

- 英文：`PolicyScope`。
- 中文：`政策涟漪`。

产品可以说明“面向中央层面政策统筹人员”，但不得使用国徽、国务院标识、红头文件、官方印章或 `Official Institutional Use Only` 等现实官方身份暗示。

真实企业名称只用于模拟主体识别。不得使用第一人称企业台词、承诺式文案或未经许可 Logo。

### 4.2 风格

风格为浅色“现代制度工作台”：克制、可信、地图优先、高信息密度。

- 左侧栏 244px，1320px 及以下收缩为 216px。
- 顶部双层：实验身份与真实 CTA；阶段与季度门禁。
- 12 列流式栅格，间距使用 4/8/12/16/24/32px。
- 页面背景 `#F7F8FB`，白色主卡，1px 低对比边框。
- 卡片 12px 圆角，控件 8px，状态标签胶囊形。
- 中文正文 Noto Sans SC，数值和英文 Inter。
- 避免暗色霓虹、玻璃拟态、巨大 KPI、装饰性光晕和企业品牌色堆叠。

### 4.3 语义色

| 语义 | 推荐颜色 | 用途 |
|---|---|---|
| 结构/导航 | `#202537` | 标题、导航、正文 |
| 主操作 | `#5548E7` | CTA、当前阶段、链接 |
| 中央政策 | Blue | 三档比例、政策 Diff |
| 环境事实 | Teal | 需求、机制、已结算结果 |
| Agent 策略 | Indigo | 省级/车企 Action、待验证建议 |
| 财政/风险 | Amber | 地方压力、非单调警告、条件状态 |
| 错误/拒绝 | `#BA1A1A` | 失败、拒绝、严重风险 |
| 页面背景 | `#F7F8FB` | 全局背景 |
| 边框 | `#E6E9F1` | 卡片、表格、控件 |

状态不得只依靠颜色表达。

### 4.4 字体层级

| 级别 | 字号/行高 | 字重 | 用途 |
|---|---|---|---|
| Display L | 32/40 | 600 | 页面标题、ΔGap 结论 |
| Display M | 26/32 | 600 | 地图结论、区域标题 |
| Headline S | 20/28 | 500 | 卡片组标题 |
| Body L | 16/24 | 400 | 关键说明 |
| Body M | 14/20 | 400 | 正文、表格、事件 |
| Label | 12/16 | 600 | 元数据、主体、状态 |
| Metric | 28/32 | 700 | 指数值 |

---

## 5. 全局信息架构

### 5.1 正式路由

| 路由 | 名称 | 进入条件 |
|---|---|---|
| `/experiments/new` | 新建中央补贴实验 | 无实验或主动新建 |
| `/experiments/:id/live` | 全国新能源汽车政策推演 | 初始指令已批准 |
| `/experiments/:id/provinces/:provinceCode` | 省级政策 Agent 详情 | 初始指令已批准；未到 Q1 时显示前置空态 |
| `/experiments/:id/intervention` | 首年年末干预审批 | 首年 Q4 Checkpoint 和建议已生成 |
| `/experiments/:id/compare` | 次年同源对照与复盘 | 双分支完成；拒绝路径显示原始方案年度结果 |

禁止 `href="#"`、假标签页、点击无响应卡片和本地变量伪路由。

### 5.2 深链

```text
?company=<automaker-id>
?evidence=<evidence-ref>
?branch=control|treatment
```

- `company` 打开车企侧栏。
- `evidence` 打开方法与证据抽屉。
- `branch` 只在已有分支的省级详情和车企侧栏生效。
- 三个参数可共存；关闭某层只移除对应参数。
- URL 可复制，刷新、前进和后退可恢复。

固定车企 ID：`byd`、`geely`、`changan`、`sgmw`、`nio`、`chery`、`leapmotor`、`seres`、`xiaomi_auto`、`li_auto`。

### 5.3 全局壳层

五路由共享：

1. 品牌与实验身份。
2. 当前政策周期、季度、分支和运行模式。
3. 连接状态、数据版本和 Evidence 入口。
4. 阶段导航与审批门禁。
5. 固定研判免责声明。

阶段条顺序：

```text
SETUP
Y1 Q1
Y1 Q2
Y1 Q3
Y1 Q4
YEAR1 REVIEW
Y2 Q1
Y2 Q2
Y2 Q3
Y2 Q4
COMPLETE
```

阶段条允许回看已完成阶段，不能绕过审批跳到未来阶段。V3 主界面不得继续使用 T0–T5。

### 5.4 页面状态机

| 状态 | 必须显示 | 主操作 |
|---|---|---|
| `loading` | 正在加载的资源名和骨架 | 无重复提交 |
| `empty` | 缺失原因与前置条件 | 返回上一步或开始 |
| `draft` | 未批准政策 | 编辑、批准 |
| `awaiting_approval` | 对象、Diff、影响范围 | 批准、修改、拒绝 |
| `running` | 当前季度、主体进度、事件 | 查看详情，不能重复运行 |
| `approved` | 审批版本和时间 | 进入下一阶段 |
| `completed` | 结果与证据 | 查看对照/复盘 |
| `fallback` | 主体、阶段、原因和接管范围 | 继续或重试 |
| `failed` | 稳定错误码和恢复建议 | 重试/返回安全状态 |
| `reconnecting` | 最后事件 ID 和重连状态 | 自动恢复、手动刷新 |

---

## 6. 公共组件

### 6.1 来源标签

必须区分：

- `中央政策`：用户批准的三档比例。
- `省级策略`：省级 Agent 的结构化 Action。
- `车企模拟`：真实企业数据基线上的模拟 Action。
- `环境计算`：确定性机制和指标。
- `用户决策`：批准、修改、拒绝。
- `真实数据基线`：公开来源的冻结事实。

数据质量使用 `verified/proxy/demo`，不得显示伪置信度。

### 6.2 六项中央指标

固定为：

1. 区域发展差距。
2. 中央财政负担。
3. 地方财政压力。
4. 新能源汽车需求。
5. 新增投资集中度。
6. 产业集聚度。

指标卡必须显示名称、`指数/100`、季度/分支、相对基线的指数点差、方向语义和环境计算标签。区域差距、财政压力和集中度的升降不能自动等同好坏，必须结合当前政策目标解释。

### 6.3 三档比例组件

- 固定顺序：西部、中部、东部。
- 默认：95%、90%、85%。
- 支持“直接输入”和“基于参考值调整”两种模式。
- 三项分别为 0–100%，不显示合计。
- 非单调时显示“实验值偏离参考梯度”警告，但不禁用批准。
- 展开后显示三档 12/10/9 省份名单和政策来源。

### 6.4 省级补贴结构

同时显示：

- 总体支持强度。
- 消费端份额。
- 固定成本份额。
- 可变成本份额。
- 当前财政空间和地方配套压力。

三类份额总和不为 100% 时显示字段错误；不静默归一化。

### 6.5 车企标记与卡片

- 使用中性圆点、缩写或文本名，不使用未经许可 Logo。
- 所有活动标记必须带“模拟”或 `AUTOMAKER SIMULATION` 标签。
- 卡片显示真实数据基线年份、经营画像、模拟 ROI 等级和 Action，不显示未来现实金额或销量。
- 产能动作翻译为“模拟新建”“模拟扩产”“延迟观察”，不得写成企业承诺。

### 6.6 事件流

- 按 `event_id` 排序和去重。
- 显示季度、主体、事实摘要、来源、时间和 fallback。
- 省级决策、车企组合、环境季度结算、Checkpoint 和审批事件可筛选。
- 不展示长思维链。

### 6.7 审批控件

- 初始审批提供“批准并开始首年”。
- 年末审批必须有“批准”“修改后批准”“拒绝”三种操作。
- 提交前显示三档比例 Diff、首年父 Checkpoint 和次年分支说明。
- 提交中锁定重复操作。
- API 403/409 显示稳定错误码。

---

## 7. 中国地图主画布

### 7.1 布局优先级

Live 首屏的视觉面积优先分配给地图：

- 1536×1024：地图区域占内容区约 60–66%，右侧为政策快照、季度事件和主体进度。
- 1440×900：地图区域不低于内容区 58%，右栏可内部滚动。
- 1280：地图保持完整宽度，上方或下方重排摘要，不横向裁切地图。

六项指标使用紧凑摘要条或可展开区，不得把地图压缩为装饰缩略图。

### 7.2 填色主图层

同一时刻只允许一个省域填色主图层：

```text
local_subsidy_intensity         默认
consumer_subsidy
fixed_cost_subsidy
variable_cost_subsidy
wtp
industry_base
```

图例必须显示：图层名、指数/份额单位、当前季度、分支、范围和空值语义。

### 7.3 覆盖图层

可独立开关：

```text
battery_nodes
automaker_sales_activity
simulated_facility_activity
```

- 电池节点使用中性电池/供应链图标和能力等级。
- 销售活动按选定车企显示点、环或强度，不同时展示十家全部文字标签。
- 产能活动使用工厂图标，必须带“模拟”语义和动作类型。
- 覆盖层不能改变省域主色阶。

### 7.4 地图交互

- Hover：省份、当前图层值、季度、质量和 fallback。
- Click：选中省份并显示省级聚焦卡。
- “查看省级详情”进入正式省级路由。
- 车企活动点可打开 `?company=`。
- 键盘可遍历 31 省和覆盖层主体。
- 缺数据使用 hatch/空值，不填 0。

### 7.5 地图技术与来源

- 使用 ECharts `registerMap` 注册项目内离线 SVG。
- Live 与 Compare 共用同一地图、代码—几何绑定和色阶逻辑。
- 继续使用自然资源部标准地图 GS(2016)1609 衍生资源及现有 provenance、SHA256 和几何签名。
- 电池和车企活动是数据覆盖层，不修改行政区边界。
- 仿真只计算中国大陆 31 个省级行政区；新疆生产建设兵团不作为独立地图区域或 Agent。

---

## 8. `/experiments/new`：中央政策设定

### 8.1 目标

用户在一个屏幕内完成：

```text
输入实验目标
  → 中央 Agent 结构化解析
  → 核对三档承担比例
  → 查看参考口径和警告
  → 批准并开始首年
```

### 8.2 布局

- 左侧 4 列：实验目标、核心问题、参考政策和科学边界。
- 右侧 8 列：三档比例、输入模式、地区清单、中央摘要和审批。
- 主 CTA 固定在可见区，不遮挡内容。

### 8.3 输入

默认目标：

> 评估调整西部、中部、东部汽车以旧换新中央承担比例后，对省际新能源汽车发展差距、财政负担和产业布局的影响。

结构化政策只包含三档比例及参考规则。消费补贴标准和资格规则在同一 A/B 中冻结，不允许在页面顺带修改其他字段。

### 8.4 参考说明

必须显示：

- “2025 年政策参考基线”，不是永久当前政策。
- 西/中/东 95%/90%/85%。
- 三档省份使用汽车以旧换新财政专项口径，而非统计四大区域口径。
- 点击可打开来源 Evidence。

### 8.5 异常

- 比例超出 0–100%。
- 非单调梯度警告。
- 中央 Schema 修复和 fallback。
- 审批 API 403/409。
- 刷新恢复 draft/awaiting approval。

---

## 9. `/experiments/:id/live`：全国地图推演

### 9.1 首屏结构

1. 紧凑六指标摘要与当前季度。
2. 地图主画布。
3. 右侧政策快照、主体进度、关键事件。
4. 页面下方季度机制趋势、三类补贴分布和车企行动摘要。
5. 年度阶段时间线。

### 9.2 季度内容

- `Y1_Q1/Y2_Q1`：省级补贴结构和 Peer 响应。
- `Y1_Q2/Y2_Q2`：十家车企全国市场与产能组合。
- `Y1_Q3/Y2_Q3`：需求、成本、供应链和财政传播。
- `Y1_Q4/Y2_Q4`：年度结算、Gap、HHI 和机制结果。
- `YEAR1_REVIEW`：首年 Checkpoint 和中央研判入口。

### 9.3 主 CTA

根据状态显示：

- 运行下一季度。
- 查看首年年末研判。
- 正在运行（禁用）。
- 恢复连接。
- 查看次年对照。
- 查看年度结果（拒绝路径）。

重复点击不得并发触发同一阶段。

### 9.4 主体进度

- 省级：`完成省份 / 31`。
- 车企：`完成车企 / 10`。
- Fallback 主体数及列表。
- 环境季度提交状态。

不在 Live 同时展开 31×10 详情卡。

### 9.5 断线恢复

- 保留最后完整 WorldState。
- 显示最后事件 ID 和重试状态。
- 重连携带 Last-Event-ID 并去重。
- 恢复后重新获取 WorldState，不仅依赖缺失事件推断状态。

---

## 10. `/experiments/:id/provinces/:provinceCode`：省级详情

### 10.1 信息顺序

```text
地方财政空间
  → 三类补贴配置
  → 本次实验省级决策画像
  → Peer Policy 响应
  → 10 家车企反馈
  → 需求、投资与机制结果
```

### 10.2 首屏

必须可见：

- 省份、政策档位和当前中央承担比例。
- 地方配套负担、剩余财政空间和总体支持强度。
- 三类补贴份额。
- Persona 六轴入口。
- Peer 响应模式和观察对象。
- 当前季度/分支和 Evidence 入口。

### 10.3 车企反馈

以 10 家中性文本卡或表格显示：

- 销售投入强度和渠道策略。
- 是否存在本省产能动作。
- 模拟 ROI 等级。
- 原因码、fallback 和 Action 深链。

点击车企打开 `?company=`，不得把模拟产能动作写成现实承诺。

### 10.4 机制

显示消费补贴、固定成本、可变成本、WTP、电池距离、Peer、车企财务约束、销售投入和产能投入贡献。固定/可变成本卡必须显示临界季度或规模指数。

---

## 11. `?company=`：车企侧栏

建议宽度 520–600px，可与 Evidence 抽屉按层级切换，不同时遮挡全部地图。

### 11.1 头部

- 企业文本名和稳定 ID。
- 固定标签“真实数据基线 / 模拟车企行动”。
- 基线年份 2025、数据质量和来源入口。
- 不使用企业 Logo、口号或品牌营销图。

### 11.2 冻结经营画像

- 销量规模与增速指数。
- 盈利、流动性和产能利用代理。
- 渠道覆盖、现有生产布局、产品与技术路线。
- 扩张/稳健/防御姿态。
- 集团/品牌/上市主体口径说明。

### 11.3 模拟行动

- 31 省销售投入地图或排序表。
- 每省 `expand/maintain/reduce`。
- 0–3 个 `new_plant/expand/delay` 模拟产能目标。
- 模拟 ROI 等级、原因码、上一行动和分支。
- 真实数据与模拟 Action 必须使用不同视觉标签。

### 11.4 固定免责声明

> 本侧栏使用公开资料构造冻结经营基线；所示销售投入和产能行动为当前机制下的模拟策略，不代表该企业的现实计划、承诺或建议。

---

## 12. `/experiments/:id/intervention`：首年年末审批

### 12.1 三栏

#### 左栏：结构化证据

- 首年 Checkpoint、版本和 seed。
- Gap、中央负担、地方压力和需求。
- 31 省三类补贴分布。
- 车企销售与产能活动分布。
- WTP、电池供应链和固定/可变成本临界点。
- Fallback、质量和 Evidence Ref。

#### 中栏：中央 Agent 建议

- 西部/中部/东部 `from → to`。
- 目标指标和待验证方向。
- 可能代价：中央负担、地方压力、需求、集中度和产业集聚。
- 证据引用。
- 不使用“最佳”“必然”“现实最优”。

#### 右栏：人工审批

- 完整 Diff 和影响范围。
- 批准、修改后批准、拒绝。
- 修改只允许三个比例，独立 0–100%。
- 非单调警告不阻断提交。
- 明确说明批准后从同一首年 Checkpoint 创建次年 Treatment。

### 12.2 状态

- 无 Checkpoint：显示前置条件。
- 建议生成中：证据保留，审批禁用。
- Fallback 建议：显示原因，允许人工审核。
- 已拒绝：不创建 Treatment，允许运行次年 Control。
- 已批准：显示用户最终值和中央原建议 Diff。

---

## 13. `/experiments/:id/compare`：次年同源对照

中性命名固定为：

- Control：原始方案。
- Treatment：干预方案。

结果完成前不得称“优化方案”。

### 13.1 信息顺序

1. 同源性证明：首年父 Checkpoint、版本、seed 规则。
2. 三档承担比例 Diff。
3. Gap、`ΔGap` 和方向解释。
4. 六项中央指标。
5. 原始/干预同步双地图。
6. 31 省三类补贴与发展指数迁移。
7. 十家车企销售和产能行动迁移。
8. 固定/可变成本临界点、机制贡献和中央复盘。

### 13.2 ΔGap

固定公式：

```text
ΔGap = Gap_treatment,Y2 − Gap_control,Y2
```

- 负值：差距缩小。
- 正值：差距扩大。
- 接近 0：当前显示精度与机制阈值内变化有限。

显示环境计算标签，不展示显著性或概率。

### 13.3 双地图

- 两图使用同一 SVG、主图层、范围、色阶和图例。
- 默认比较地方补贴支持强度，可同步切换其他图层。
- Hover 和选择同步。
- 点击省份进入省级详情并带分支参数。
- 车企覆盖层同步选择同一车企。
- 未完成分支显示状态层，不以 0 伪装结果。

### 13.4 车企行为迁移

至少显示：

- 31 省销售投入强度差。
- `expand/maintain/reduce` 迁移。
- `new_plant/expand/delay/hold` 迁移。
- 主动政策差异、环境传播和 fallback 标签。

### 13.5 拒绝路径

如果年末建议被拒绝，Compare 路由显示“次年原始方案年度结果”，明确无 Treatment、无 ΔGap，不绘制伪双地图。

---

## 14. `?evidence=`：方法与证据抽屉

建议宽度 500–580px，提供：

1. **行为链**：输入哈希 → 模型/规则 → 校验/修复 → Action → 事件。
2. **机制链**：公式、输入、系数、贡献、裁剪、结果和守恒。
3. **版本与来源**：政策、地区档位、WTP、电池节点、车企数据、地图、模型、Prompt、Schema、机制、app、seed 和 Checkpoint。

支持 `audit:`、`action:`、`automaker:`、`mechanism:`、`metric:`、`checkpoint:` 和 `comparison:`。

必须明确“不展示模型内部思维链”，不得显示 API Key、Authorization、token、secret、原始无效响应或 `reasoning_content`。

---

## 15. 数据与文案规则

### 15.1 数值

- 模拟指标：最多一位小数，`72.4 / 100`。
- 指数差：`+3.2 指数点`。
- 中央承担比例和工具份额：百分比。
- Gap 和 HHI：指数/100，不写成现实比例。
- 时间：本地显示，Evidence 保留 UTC 原值。

### 15.2 必须使用

- “2025 年政策参考基线”。
- “模拟指数变化”。
- “待验证”。
- “原始方案 / 干预方案”。
- “真实数据基线 / 模拟车企行动”。
- “本次实验省级决策画像”。
- “规则接管：该主体行动由确定性规则生成”。

### 15.3 禁止使用

- “当前国家永久比例”。
- “现实最优比例 / 最佳方案 / 优化方案”。
- “某车企将建厂 / 承诺投资 / 决定扩产”。
- “销量将增长 X 辆/X%”。
- “利润增加 X 亿元”。
- “新增投资 X 亿元”。
- “中央一定节省/地方一定受益”。
- “国务院认为 / 某省政府决定”。
- “AI 置信度 96%”。
- `Official Institutional Use Only`。

---

## 16. React 实现约束

- 使用 React Router 管理五路由和三类 query 深链。
- 页面通过统一 API client/hooks 获取 DTO。
- SSE 由专门 hook 管理去重、重连和状态刷新。
- Server State 与地图图层、侧栏、筛选等 UI State 分离。
- 不在浏览器重算 Gini、HHI、财政、ROI 或机制结果。
- 地图、图表和长列表按路由懒加载。
- 图层切换只改变视图，不触发 Agent 或 API 运行阶段。
- 不把 31 省和十家车企 WorldState 复制到多个不同步 store。
- 不复用 V2.1 静态结果或通过字段改名伪装 V3 DTO。

---

## 17. 可访问性

- 中文优先，只保留 Agent、Control/Treatment、ID 等必要机器概念。
- 所有图标按钮有可访问名称和 tooltip。
- 输入有关联 label、帮助和错误信息。
- 键盘可完成比例输入、地图选省、车企侧栏、Evidence、审批和 Compare 图层切换。
- 31 省和覆盖层提供文本/表格替代。
- 状态、风险、分支和图层不能只靠颜色区分。
- 动效遵守 `prefers-reduced-motion`。
- 文本与背景达到 WCAG AA 对比度目标。

---

## 18. V3 Design QA

### 18.1 截图矩阵

| 页面/状态 | 1536×1024 | 1440×900 | 1280 |
|---|---:|---:|---:|
| New draft | 必须 | 必须 | 必须 |
| New 非单调警告 | 可选 | 必须 | 可选 |
| Live Y1_Q1 | 必须 | 必须 | 必须 |
| Live Y1_Q2 + 车企覆盖层 | 必须 | 必须 | 必须 |
| 省级详情 | 必须 | 必须 | 必须 |
| 车企侧栏 | 必须 | 必须 | 必须 |
| 年末审批 | 必须 | 必须 | 必须 |
| Compare completed | 必须 | 必须 | 必须 |
| Evidence | 可选 | 必须 | 可选 |
| Fallback/Failed/Reconnecting | 可选 | 必须 | 可选 |

### 18.2 比较方法

现有 Stitch 图只用于布局和视觉节奏对照；V3 业务语义以 PRD 为准。每个核心页面必须：

1. 截取源参考、V3 设计目标和正式实现。
2. 在同一画布比较布局、地图面积、间距、字体、状态、图层和首屏可达性。
3. 记录 P0/P1/P2/P3 差异。
4. 修复 P0/P1/P2 后重新截图。
5. 检查真实企业模拟边界、禁止文案和地图来源。

### 18.3 `design-qa.md`

V3 章节必须记录：

- 基准图与实现图路径。
- viewport、浏览器、构建和数据版本。
- 页面阶段、分支和测试数据集。
- P0/P1/P2/P3 差异和修复引用。
- 地图、企业数据和政策来源检查。
- 可访问性、禁止文案和 Logo 扫描。
- `V3 final result: passed | failed`。

V3 实现证据不完整时只能标记 `not started`，不得沿用 V2 或 V2.1 结论；当前证据已记录在 `design-qa.md`。

---

## 19. 自动化验收清单

### 路由与门禁

- [x] 五个路由可直接访问并恢复状态。
- [x] `company/evidence/branch` 参数可共存。
- [x] 未批准初始政策不能进入首年。
- [x] 未批准年末建议不能创建干预方案。
- [x] 拒绝路径不显示伪 Treatment。

### 政策与阶段

- [x] 西/中/东默认 95%/90%/85%。
- [x] 三项独立 0–100%，非单调只警告。
- [x] 地区档位显示 12/10/9 和正确省份。
- [x] 阶段使用 SETUP、Y1/Y2 季度和 COMPLETE，不使用 T0–T5。

### 地图与主体

- [x] 31 省完整、唯一、可点击。
- [x] 默认图层为地方新能源汽车补贴支持强度。
- [x] 六个填色层和三个覆盖层真实工作。
- [x] 十家车企侧栏均可打开。
- [x] 所有车企活动明确标记模拟且无未授权 Logo。

### 指标与对照

- [x] 六项中央指标名称和单位一致。
- [x] 省级发展指数为需求/产业各 50%。
- [x] Gap/ΔGap 公式和符号说明正确。
- [x] HHI 只显示环境计算结果。
- [x] Compare 首先显示同源、比例 Diff 和 ΔGap。

### 状态与可靠性

- [x] Loading/Empty/Running/Awaiting Approval/Completed 可见。
- [x] Fallback/Failed/Reconnecting 可恢复且不静默。
- [x] Last-Event-ID 和去重不产生重复事件。
- [x] 图层切换不触发模型调用。

### 视觉与可访问性

- [x] 地图在首屏保持主视觉面积。
- [x] 1536×1024、1440×900、1280 无核心遮挡和水平滚动。
- [x] 键盘、焦点、对比度和图表替代通过。
- [x] 无 CDN、iframe、截图地图和 `href="#"`。
- [x] 禁止预测、承诺、最优、官方暗示和伪置信度扫描通过。
- [x] `design-qa.md` 的 V3 最终结果为 `passed`。

---

## 20. 前端完成定义

V3 前端只有同时满足以下条件才完成：

- 五路由、车企侧栏和 Evidence 抽屉接入正式 V3 API/SSE。
- 31 省地图和九类图层使用真实 V3 DTO。
- 三档比例、地区档位、季度阶段和两次审批不可绕过。
- 31 省和十家车企完整，fallback 可见。
- Compare 来自同一首年 Checkpoint，先显示 ΔGap 和同源证明。
- 所有真实企业行为明确为模拟，不使用未授权 Logo或现实承诺。
- 完整状态、单位、来源和免责声明符合 PRD。
- 三种桌面画布、核心 E2E、禁止文案和可访问性检查通过。
- `design-qa.md` 的 V3 `final result` 为 `passed`。

上述条件已由正式 React 运行时、V3 API、两条 Playwright 流程和 1536×1024、1440×900、1280 三画布截图验证。V3.0 前端状态为“已实现，Design QA passed”。

---

## 21. V3.1 五路由增量

- New 在实验目标与政策草案之间加入两种比较模式选择，并用一句短标签说明唯一主动差异。
- Intervention 的政策模式保留比例审批；事件模式改为首年脆弱性证据、同政策证明和双分支确认，不展示可编辑比例。
- Live 在两分支完成 Y2_Q2 后显示事件实验台：模板、强度、机制预览、不可逆批准按钮；批准前不得进入 Y2_Q3。
- Live 地图新增 `event_exposure` 填色层和 `province_interaction` 响应层；协作清单保留 matched/unmatched，不把单向提议画成成功关系。
- Province 在常规财政、补贴、Persona、Peer、车企和机制之后显示“事件暴露 → 首轮信号 → Peer 响应 → 协作状态”链。
- Compare 根据模式使用“原始方案/干预方案”或“无事件基线/事件情景”，在地图和指标之前显示 Checkpoint、`policy_diff`、`event_diff` 与唯一主动差异证明。
- 事件实验台固定显示情景免责声明；不得使用现实油价预测、现实法规结论、企业承诺或官方立场表达。
- 不新增正式路由；loading、empty、error、fallback、重连和 Evidence 深链继续沿用五路由运行时。

V3.1 前端只有两条模式 E2E 和三画布检查完成后才能标记 Design QA passed；V3.0 的历史截图不能替代。

---

## 22. M30 策略市场与决策边界展示

- Live 在省级调整轮展示“企业反馈 → 省级提议 → 接受/拒绝 → 匹配”，数据来自 `strategy-market-v1`，不由前端拼造成功合作。
- Province 顺序为事实与代理特征、目标与资源包、初始方案、Peer/企业信号、提议与响应、为什么/为什么不/改变条件、机会成本、Delta 与环境影响。
- Automaker 抽屉展示全国市场资源、渠道扩张名额、设施资源、31 省候选/观察/暂不考虑评价、资源取舍和重新考虑条件。
- Compare 三条机制固定为主要正向机制、主要代价机制和结论反转风险；每个节点可下钻至 Policy、Action、Match、环境贡献或 Metric。
- Methods 显示 Agent 模型/回放来源、调用数、fallback 数、资源公式、输入/输出哈希口径、Replay、Audit 和 Evidence。技术字段不进入主页。
- `proxy` 在主页统一显示“代理数据基线”，不得显示为“真实数据基线”或模糊的“可信数据”。

## 23. M31 互动决策台与差异地图

- Live 首屏采用“地图主画布 + 右侧互动决策台”，顶部使用五轮紧凑进度轨；不再显示占用首屏高度的纵向时间线。1536×1024 和 1440×900 两者同屏；1280 以下地图优先、互动台纵向排列，禁止横向滚动。
- 地图默认显示“干预方案 − 原始方案”，以真实数据动态对称缩放；图例必须说明单位、最大差异和正负方向。单方案绝对图用 5%–95% 分位色阶。未生成的值显示纹理空态，不写作 `0`，不参与色阶。
- 互动台在“省际协同 / 省企撮合”之间切换，始终显示总量、生效、拒绝或未生效状态以及当前链路。选中条目高亮省份，并可进入省份详情或车企抽屉。
- 省企资源包的用户文案必须使用“模拟协同资源包”；明确显示车企接受或拒绝及零贡献无效状态，不得写成现实招商、供应合同、建厂或投资承诺。
- Province 展示省际提议和省企资源包的独立限额、发起/不发起、接受/拒绝、机会成本、改变条件和生效影响。Automaker 抽屉展示 31 省明确投入决定、主承诺及逐项省企响应；不得展示“候选省份”“候选池”“观察/淘汰候选”等主结果。
- 所有操作区只保留一个主按钮；按钮统一至少 38px 高，正文不低于 14px、标签不低于 12px，禁用态保持可辨识文字和对比度。

## 24. M32 / v9 竞争与谈判画布

- Live 顶部为七轮紧凑进度轨。地图继续以省域填色为主画布，叠加红色竞争损失边、青色协同边和靛色 Top-K 资源流；默认限制边数，选中省份或车企后显示其完整关联链。
- 右侧决策台提供“竞争反制 / 省企谈判 / 省际协同”三个筛选视图。点击记录联动地图与 Province/Automaker 深链；竞争记录必须展示赢家、落选省、资源类型、名次和模拟损失，不得暗示现实投资承诺。
- Province 显示效用六项拆解、Top-K 挤出、反制前后策略、反报价的接受/拒绝与机会成本。Automaker 抽屉显示 Persona 决定的 K、初步/最终名额、反报价和被拒后的重配。Compare 增加竞争损失、Top-K 重配、反报价成交率与协同收益的分支对比。

## 25. M33 独立全景推演厅

### 25.1 模块与页面形态

- 新建独立 `apps/presentation`，入口 `/experiments/:id/present`；旧多路由 React 工作台只作迁移基线和故障回退。
- 全景推演厅没有常驻 SaaS 侧栏、页面级表单页或传统卡片瀑布。全部业务通过顶部 HUD、地图、左侧叙事浮层、右侧功能坞、浮动 Tab、模态窗和底部时间轴完成。
- 地图占可见面积 70%–80%；默认首屏只保留当前阶段、一句结论、三条关键变化和时间轴。
- 固定模式为“实时推演 / 章节回放 / 结果对照”。结果默认单图 Delta，可切换同步 A/B。
- 首次进入先播放 3–5 秒深空地球开场：地球缓慢旋转，镜头向中国推进并点亮 31 省，最终在同一构图重心上淡入全国省域推演地图。开场不得承载业务数值或伪造运行进度。
- 根入口不设置开场前菜单。全国地图接管约 2 秒后自动打开玻璃配置弹窗；“政策比例”是默认首 Tab，配置东中西中央承担比例，“突发事件”是第二个可选 Tab，默认不注入且不阻塞纯政策对比启动。
- 配置提交后使用同一空间玻璃容器顺序展示中央解读、A/B 设计与唯一主动差异、代理数据基线三个人工确认状态。不显示原始 Checkpoint ID、seed 或 hash，只用“同一冻结基线派生”表达业务真相。
- 组件语言统一为 GovSim Glass UI Kit。只允许 Floating Glass Panel、Glass Pill/Chip、Floating Segmented Control、单指标 Metric Card、Context Popover、Timeline Rail、Command/Scenario Bar 与 Bottom/Side Sheet 使用玻璃材质；不得出现规则卡片矩阵或大面积模糊背景。

### 25.2 突发事件模块

- 右侧功能坞固定提供“事件”。面板显示模板、类别、强度、触发边界、提前通知、影响主体、机制通道、分支范围和待验证方向。
- 首批显示现有五项正式模板；目录 UI 必须支持未来按版本扩展，并将尚未实现机制的候选项标记为“尚未支持”。
- “国际冲突情景下油价上涨”可使用“伊朗相关冲突升级导致油价上涨”作为示例说明，但固定显示情景免责声明，不使用现实新闻直播视觉或结论式预测。
- 事件发生时，左侧叙事浮层切换为事件标题、强度、直接暴露主体和传播进度；地图增加事件暴露和传播覆盖层。
- 时间轴使用菱形事件节点。点击节点聚焦事件影响，不能修改已经冻结的事件或历史 Action。

### 25.3 可拖动时间轴

- 时间轴包含方案冻结、七轮、环境结算和结果复盘；事件节点插入轮次边界。
- 使用可拖动滑轨、吸附节点、播放/暂停、前后步、`0.5x/1x/1.5x/2x`、进度和复位。
- 拖动时暂停；Live 不能越过最新完成帧；Story/Compare 可访问全部已完成帧。
- 业务状态仅在合法帧切换。帧间动画只作用镜头、填色、关系线、标记、浮层和数值显示，不插值权威业务值。
- 默认动画顺序为镜头聚焦、填色、关系线、主体标记、关键变化、数字；低动效模式取消镜头飞行和路径绘制。

### 25.4 浮层与交互

- 右侧一级入口固定为方案、事件、省份、车企、竞争、谈判、协同、结果、方法和图层。
- 同时只打开一个主面板；Evidence 是二级抽屉。任一状态下地图至少保留 55% 可见宽度。
- Context Popover 负责地图对象的第一层短反馈；Floating Glass Panel 只展示一个主体动作和最多三个变化；Bottom/Side Sheet 才承载完整政策配置、互动、指标与 Evidence。
- 省份、企业、竞争线、协同线、Top-K 流和事件节点互相联动，并保持当前分支、帧和选择状态。
- 空格播放/暂停，左右键逐帧，`Shift+左右` 跳章节，`F` 全屏，`Esc` 收起浮层。
- 开场提供“跳过开场”，进入主舞台后提供“重播地球开场”；`prefers-reduced-motion` 下取消旋转和长距离飞行，以不超过 1 秒的聚焦淡入完成交接。

### 25.5 目标画布与可靠性

- 正式验收画布新增 1920×1080、2560×1440、3840×2160，同时保留 1536×864 和 1366×768 的操作验证。
- 主引擎使用本地 MapLibre/deck.gl；WebGL 失败时降级为现有 ECharts/SVG 地图，不影响时间轴和业务操作。
- 全国地图必须完整显示香港、澳门、台湾的轮廓和名称。三者使用与 31 省相同的冻结标准地图来源，但采用独立的中性 `territory-context` 样式；不响应 hover 选中、键盘聚焦或点击下钻，不进入当前指标色阶，并在“方法与数据”中说明“31 省参与推演，港澳台仅作中国版图展示”。
- 所有地图、字体、图标和演示资产本地打包，无 CDN、远程底图、iframe 或截图地图。
- 默认玻璃 Token 为深色半透明底 `rgba(18,18,22,.55)`、`24px` blur、`140%` saturate、`rgba(255,255,255,.10)` 细边、`22px` 圆角、外部深影与 `inset` 顶部高光；必须同时依靠透明度、模糊、边缘高光和内容层级，不得只套 `backdrop-filter`。
- 第一层只显示短标签、主体动作和一个大数字；第二层显示指标关系；第三层才显示方法、数据、Agent 结构化理由与 Evidence。不得把日志或长解释直接铺在地图上。
- 断线时保留最后完整帧，重连后先取 Presentation Timeline 和当前 Frame，再恢复 SSE。
- M33.1 已验证本地 MapLibre/deck.gl 的 31 省绑定、Delta、弧线、4K 连续播放、低动效和 SVG 容错；M33.2 两个只读投影接口、10 帧完整时间轴、事件节点、覆盖层和 Replay 映射也已通过，正式页面可以开始接入。
- M33.0–M33.6 已在 `apps/presentation` 全部冻结：五项事件目录和三触发边界可在单屏设定，七轮由 SSE 事实流逐轮推进；Story 使用后端五幕摘要，Compare 提供权威 Delta 和同步 A/B；键盘遥控、断网保帧、Fake/Fallback 标签、WebGL→SVG 降级与 1080p/2K/4K 已通过正式 E2E 和 Design QA。

## 26. M34 Presentation Hall V2 前端规范

- M33 V1 已被 breaking upgrade 取代。主演示厅只开放 Live/Compare，当前帧完整数据懒加载并预取相邻帧；时间轴不得重新内嵌完整投影。
- 左侧主浮层是 Game Spotlight，不再以三条结果摘要替代主体决策。固定显示主体、目标、最强约束、观察事实、合法策略评分、实际选择、机会成本和实际回应/待回应。
- 微镜头顺序固定为聚焦、观察、选择板、实际行动、实际回应、取舍/分歧；只重组当前冻结帧数据，不插值业务值。
- 单图突出当前 Thread，其余关系降级。Control 必须同时使用靛色、虚线、空心；Treatment 必须同时使用青色、实线、实心；每处还要保留文字标签，满足非颜色识别。
- 首次关键分歧、重大目标切换和环境结算允许自动进入同步 A/B，顶部始终提供“单图聚焦 / A/B 同步”直接切换。
- 时间轴上方增加行动—回应轨，以短标签显示报价、反报价、接受/拒绝、匹配和重配；点击回到对应互动入口。
- “全部决策”作为功能坞入口，按需获取全部帧并去重约 246 条轨迹，可按轮次、分支、主体、互动类型和状态筛选，条目支持键盘聚焦与 Evidence 下钻。
- 断线保留当前完整帧，新增帧按顺序播放；地图实例不能因 Frame、Spotlight、Side Sheet 或 SSE 更新重建。SVG 降级必须保留省域键盘、分支线型/标记语义与全部业务控制。

## 27. M34 季度事件驱动前端规范

- 本节取代固定七轮的活动展示语义。普通工作台与 Presentation 均以一年、Q1–Q4 和季度内 Wave 表达进度；禁止把旧轮次重新命名成季度。
- 配置页允许 0–3 个冻结事件，每项选择季度、`wave_0|wave_1|wave_2`、分支范围和提前通知；重复模板或冲突事件即时显示服务端校验错误。
- 主 CTA 使用“运行 Q1”或“运行下一季度”。Live 只展示最后完整冻结 Tick；断线不推断未提交季度。
- 时间轴必须显示四个季度区段，并固定提示“模拟季度与互动顺序，不代表现实响应日期”。政策冻结、外生事件、有互动的 Wave、季度结算和年度比较各形成聚合节点。
- 展开 Wave 节点后展示完整提议、回应、反报价、接受/拒绝/暂缓、资源无效和 fallback；同一 Wave 默认按类别聚合，不能依赖执行顺序讲述因果。
- 地图同一时刻只突出 1–3 个由后端稳定选择的关键互动，其余进入省份、车企、互动和方法面板。Spotlight 不得读取未来季度事实。
- 工作台主产品仍只展示方案、最终行动和结果；调用预算、哈希、Schema、Inbox 权限与资源守恒细节只进入“方法与数据”。
- 旧 `exp_m32_*` 深链显示稳定的“该历史运行版本已停止支持”410 状态，不尝试加载或转换。
