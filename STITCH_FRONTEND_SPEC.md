# PolicyScope V2.1 Stitch 正式前端规范

> 产品：PolicyScope / 政策涟漪  
> 规范版本：V2.1（省级 Agent 主决策，实施中）
> 更新日期：2026-08-12  
> 主验收画布：1440 × 900  
> 当前门禁：V2 前端与 Design QA 已完成；V2.1 文档已批准，五路由与省级优先体验正在实施

---

## 1. 规范目的

本文件把 Stitch 五张视觉稿转化为 V2.1 正式 React 产品的页面、交互、状态和验收契约。它不是路演脚本，也不是静态页面重制说明。

正式前端服务于国务院层面的政策统筹人员，必须帮助用户完成四件事：

1. 设定并批准中央制造业设备更新政策。
2. 观察 31 个省级 Agent 如何依据稳定决策画像形成地方工具与省际策略。
3. 查看 186 个企业群体如何反馈并验证地方政策。
4. 基于省级复盘与企业证据审批、修改或拒绝中央 Agent 的干预建议。
5. 比较同源原始方案与干预方案，并先追溯省级策略迁移，再追溯企业行为和机制贡献。

视觉质量与产品真实性同等重要。任何看似完整但没有真实路由、API、审批、状态或地图的数据大屏，都不满足本规范。

---

## 2. 适用范围与门禁

### 2.1 P0

- 桌面 Web，1440 × 900 主画布。
- 1280 像素宽仍可完成主流程。
- 五个正式路由、一个可深链证据抽屉和旧省份 query 兼容导航。
- 消费 `province-persona-v1`、`province-profile-v3`、`province-action-v3`、`province-feedback-v3`、`world-state-v3`、`comparison-v3` 和 `event-v3`；中央 Policy 与企业 DTO 继续使用 V2。
- 创建政策、审批、实时推演、干预审批、A/B、证据追溯。
- 31 省真实矢量地图、31 个实验决策画像、结构化省际策略和六类企业群体反馈。
- loading、empty、running、awaiting approval、approved、completed、fallback、failed、reconnecting 状态。
- 完整 A/B 预算仍为中央 3 次、省级约 124 次、企业约 93 次分省批量调用；Persona 与 UI 刷新不得增加模型调用。

### 2.2 非 P0

- 移动端和窄屏完整适配。
- 复杂地图飞线、3D 地球或装饰性动效。
- 独立 Replay 管理页。
- 自由拖拽大屏和自定义仪表盘。
- 园区、投资机构、公众或真实企业页面。
- 路演逐字稿、演示脚本或自动播放。

### 2.3 实施状态

V2.0 规范已获得用户批准并完成正式实现，历史对照结果见 `design-qa.md`。本 V2.1 规范已经用户批准，React、API 与视觉实现正在迁移；V2.1 完成后必须重新生成独立 QA 结论，不能沿用 V2 的 `passed`。

---

## 3. 设计来源和优先级

### 3.1 来源映射

| Stitch 来源 | 正式产品用途 | 备注 |
|---|---|---|
| `stitch_policyscope/_1/screen.png` | `/experiments/new` | 中央目标、政策参数、审批 |
| `stitch_policyscope/_2/screen.png` | `/experiments/:id/live` | 全国实时推演台 |
| `stitch_policyscope/_3/screen.png` | `/experiments/:id/provinces/:provinceCode` | 独立省级 Agent 详情页的卡片密度和三栏布局基准；内容语义按 V2.1 重排 |
| `stitch_policyscope/_4/screen.png` | `/experiments/:id/intervention` | 证据 → AI 建议 → 人类审批 |
| `stitch_policyscope/a_b/screen.png` | `/experiments/:id/compare` | 双分支对照与中央复盘 |
| `stitch_policyscope/policyscope/DESIGN.md` | 设计令牌与整体语言 | 需服从本规范的语义修正 |
| 各目录 `code.html` | 尺寸、密度和布局参考 | 不作为正式运行代码 |

审查过程的辅助截图位于 `stitch_policyscope/audit/`，用于记录折叠区、抽屉和滚动后的状态，不作为第六个页面。

### 3.2 优先级

冲突时按以下顺序：

1. `PRD_省域政策多智能体推演平台.md`
2. 已冻结的领域 Schema 和 API DTO
3. 本文件
4. `DESIGN.md` 与五张 `screen.png`
5. Stitch `code.html`

例如：Stitch 页面若出现现实金额、伪精确结果、错误 Agent 归因或官方身份暗示，必须使用 PRD 和本规范修正，不能为了像素一致保留错误。

---

## 4. 品牌与视觉语言

### 4.1 品牌

全站只使用：

- 英文：`PolicyScope`
- 中文：`政策涟漪`

不得出现“政策汹涌”“政策浪潮”“政策汹浪”等变体。不得使用国徽、国务院标识、红头文件样式、官方印章或 `Official Institutional Use Only` 等现实官方身份暗示。

产品可说明“面向国务院层面政策统筹人员”，但 Agent 文案必须写成：

- “中央政策研判 Agent 在当前实验中提出”
- “省级响应 Agent 选择”
- “企业群体 Agent 反馈”

不得写成“国务院认为”“某省政府决定”或“企业承诺”。

### 4.2 风格

风格定义为浅色“现代制度工作台”：高信息密度、结构清晰、克制可信。避免深色霓虹、玻璃拟态、科技感光晕和装饰性大屏。

核心特征：

- 12 列流式栅格。
- 32px 画布边距，24px 栅格间距，8px 基础间距单位。
- 白色和浅蓝灰分层表面。
- 1px 低对比边框和克制阴影。
- 卡片 8px 圆角，主要按钮 12px 圆角，状态标签胶囊形。
- 数值、英文标签使用 Inter；中文正文使用 Noto Sans SC。

### 4.3 核心色彩

CSS Token 应从 `DESIGN.md` 派生并集中定义，禁止组件内散落硬编码。以下语义优先：

| 语义 | 推荐颜色 | 使用范围 |
|---|---|---|
| 结构/导航 | `#0B1F33` / `#081D30` | 标题、主导航、主要文本 |
| 主操作 | `#2563EB` | 主 CTA、当前步骤、链接 |
| 证据/环境计算 | `#0F766E` | 数据来源、环境结果、已验证事实 |
| AI/待验证 | `#6366F1` | Agent 解释、建议、待验证标签 |
| 风险/财政压力 | Amber | 警告、财政压力、条件参与 |
| 错误/拒绝 | `#BA1A1A` | 失败、拒绝、严重风险 |
| 页面背景 | `#F8F9FF` | 全局背景 |
| 卡片 | `#FFFFFF` | 主容器 |
| 边框 | `#E2E8F0` | 卡片、表格、输入框 |

禁止只用颜色表达状态；所有状态必须同时包含文本和图标。

### 4.4 字体与图标

- Inter 和 Noto Sans SC 必须随项目本地打包或由受控本地字体栈提供。
- 运行时不得依赖 Google Fonts。
- 图标使用本地 Material Symbols 类资源或选定的线性图标库。
- 默认图标 20px、1.5px 视觉线宽；活动状态可使用实心变体。
- 不使用 emoji、文本字符或临时手绘 SVG 冒充正式图标。

### 4.5 字体层级

| 级别 | 字号/行高 | 字重 | 用途 |
|---|---|---|---|
| Display L | 32/40 | 600 | 页面主标题、全国关键结论 |
| Display M | 26/32 | 600 | 区域标题、主要阶段结果 |
| Headline S | 20/28 | 500 | 卡片组标题 |
| Body L | 16/24 | 400 | 关键说明 |
| Body M | 14/20 | 400 | 正文、表格、事件 |
| Label | 12/16 | 600 | 元数据、Agent 类型、状态 |
| Metric | 28/32 | 700 | 指数值 |

中文内容不得因继承英文字体而出现字形和行高漂移。

---

## 5. 全局信息架构

### 5.1 正式路由

| 路由 | 名称 | 进入条件 |
|---|---|---|
| `/experiments/new` | 新建中央政策 | 无实验或主动新建 |
| `/experiments/:id/live` | 31 省政策决策与企业反馈 | T0 指令已批准 |
| `/experiments/:id/provinces/:provinceCode` | 省级 Agent 详情 | T0 已批准、Persona 已就绪；T1 未运行时显示地方决策空状态 |
| `/experiments/:id/intervention` | T3 干预审批 | 已生成 T3 Checkpoint 和建议 |
| `/experiments/:id/compare` | A/B 对照与中央复盘 | Treatment 已创建且两分支运行 |

使用 React Router。禁止 `href="#"`、假标签页、点击后无响应的卡片或通过本地变量伪装路由。

### 5.2 深链与抽屉

- `?evidence=<evidence-id>`：打开方法与证据。
- V2 的 `?province=<adcode>` 继续兼容，但必须导航到 `/experiments/:id/provinces/:provinceCode`；刷新后只保留正式路由，不继续打开旧抽屉。
- 省级详情支持 `?branch=control|treatment`。Live 使用当前活动分支且可省略；Compare 必须带分支，缺省为 `control`。分支与 evidence 参数可以共存，非法分支显示稳定错误状态。

抽屉要求：

- URL 可复制、刷新可恢复、浏览器前进后退可用。
- 关闭时只移除对应 query 参数，不破坏当前路由和其他参数。
- Esc、遮罩点击和关闭按钮均可关闭；审批提交中不可误关。
- 打开后焦点移入，关闭后焦点回到触发控件。
- 移动页面时背景滚动锁定。

### 5.3 全局壳层

五个路由共享：

1. 顶部产品栏：品牌、实验名称/ID、运行模式、数据质量入口、连接状态、用户菜单占位。
2. T0–T5 阶段条：显示当前阶段、已完成阶段、待审批门禁和分支状态。
3. 页面标题区：标题、说明、实验/分支/时间/版本元数据。
4. 免责声明：关键页面可见，不埋在帮助页。

阶段条不是可随意跳转的步骤器：用户可回看已完成阶段，但不能绕过审批进入未来阶段。

### 5.4 页面状态机

| 状态 | 必须显示 | 主操作 |
|---|---|---|
| `loading` | 骨架屏、正在加载的资源名 | 无破坏性操作 |
| `empty` | 缺少内容的原因和开始入口 | 创建/返回上一步 |
| `draft` | 未批准政策或建议 | 编辑、批准 |
| `awaiting_approval` | 审批对象、差异、影响边界 | 批准、修改、拒绝 |
| `running` | 当前阶段、完成进度、实时事件 | 查看详情，不能重复提交 |
| `approved` | 审批人/时间/版本 | 进入下一阶段 |
| `completed` | 结果和证据入口 | 查看 A/B/复盘 |
| `fallback` | 范围、原因、替代策略、可重试条件 | 继续或重试 |
| `failed` | 稳定错误码、可恢复建议 | 重试/返回安全状态 |
| `reconnecting` | 最后事件 ID、重连状态 | 自动恢复，允许手动刷新 |

全屏空白、无限 Spinner、静默降级和 Toast 一闪而过都不算合格状态处理。

---

## 6. 公共组件规范

### 6.1 状态标签

状态标签由图标、文本和低饱和背景组成：

- 草稿
- 待审批
- 运行中
- 已批准
- 已完成
- Fallback
- 失败
- 正在重连
- 待验证

不得用“置信度 92%”替代质量和状态。

### 6.2 来源标签

必须区分三类信息：

- `模型策略`：Agent 的结构化选择或解释，使用靛色。
- `环境计算`：确定性公式计算的指标和贡献，使用青色。
- `用户决策`：批准、修改、拒绝，使用蓝色或中性强调。

数据质量只使用：

- `verified`
- `proxy`
- `demo`

类别必须可打开证据抽屉；不显示伪置信度。

### 6.3 指标卡

全国固定六项指标：

1. 企业参与指数
2. 设备更新意愿指数
3. SME 融资可达性指数
4. 产业升级指数
5. 地方财政压力指数
6. 区域差距指数

指标卡必须包含：

- 名称
- 当前值，格式为 `72.4 / 100`
- 阶段与分支
- 相对基线的“指数点变化”
- 方向和好坏语义，财政压力/区域差距的下降通常为改善
- 环境计算标签
- 点击打开相应 evidence drawer

只有政策参数占比可显示 `%`。禁止把指数变化写成增长率或现实政策效果预测。

### 6.4 Agent 卡与事件流

- Agent 标签固定为 `CENTRAL`、`PROVINCIAL`、`ENTERPRISE`，旁边显示中文角色。
- 事件流按 `event_id` 排序并去重。
- 每条事件显示阶段、主体、事实摘要、来源标签、时间和 fallback 状态。
- 省级决策完成、调整意向和省际策略变化优先于企业批量事件展示；用户仍可按主体筛选。
- 长列表使用虚拟化或增量加载，不能因 186 个主体卡顿。
- 不展示长思维链；只展示结构化原因码和公开摘要。

### 6.5 省级决策画像

- 用户可见名称固定为“本次实验决策画像”，不得简写为现实“省份性格”。
- 主类型、辅助类型、六项 0–1 决策轴、优先目标、关键约束、Profile/Network/method version 和 `proxy/demo` 必须同时可追溯。
- 六轴图必须提供文本列表替代；轴值是 31 省相对百分位，不是置信度或现实评分。
- 河南、广东、山西分别用于普惠扩散、技术跃迁、绿色转型的演示验收，但页面不得硬编码结果或现实立场。

### 6.6 审批控件

- 批准、修改、拒绝必须是三个明确操作，不能只提供“应用”。
- 提交前显示政策字段 diff 和影响范围。
- 提交中锁定重复操作并保留页面上下文。
- API 拒绝必须显示稳定错误码和原因。
- 未审批时所有后续入口既要前端门禁，也要依赖服务端 403/领域错误。

### 6.7 数值与格式

- 指数：最多 1 位小数，例如 `72.4 / 100`。
- 指数差：带符号，例如 `+3.2 指数点`。
- 0–1 政策参数可在编辑器显示为百分比，例如 `60%`，持久化仍是 0–1。
- `support_intensity` 显示为 `70 / 100`，不是 `70%`。
- 时间显示本地时区，但 evidence drawer 保留 UTC 原值。
- 不显示亿元、产值增幅、就业人数或生产率预测。

---

## 7. `/experiments/new`：中央政策生成与确认

对应 Stitch `_1`，目标是让用户在一个屏幕内完成“目标输入 → Agent 解析 → 参数核对 → 批准”。

### 7.1 1440 × 900 布局

- 左侧 4 列：中央目标输入、结构化目标、场景说明。
- 右侧 8 列：完整政策参数、约束校验、中央 Agent 公开摘要、审批区。
- 页面底部或右下固定区域：主要 CTA，不被滚动区遮挡。
- 阶段条明确 T0 和“等待用户批准”。

### 7.2 中央目标输入

输入支持自然语言目标，但页面必须明确这是“实验目标”，不是向现实国务院提交政策。

结构化目标包含：

- 设备更新
- SME 参与
- 绿色转型
- 就业稳定
- 区域可达性

中央 Agent 生成后，结构化结果可人工编辑，保留生成版本和修改 diff。

### 7.3 完整政策参数

必须同时展示并允许在 Schema 范围内编辑：

- 支持强度：0–100，默认 70。
- 地方配套要求：0–1，默认 0.50。
- 工具组合：直接补贴 0.45、贷款贴息 0.35、融资担保 0.20。
- SME 倾斜：0–1，默认 0.60。
- 区域支持偏向：-1–1，默认 0。
- 技术组合：数字化 0.40、绿色 0.30、基础技改 0.30。

两个组合分别实时显示总和。总和不为 1 时：

- 显示字段级错误和当前总和。
- 禁用批准按钮。
- 不静默归一化。

### 7.4 操作

1. 用户输入目标并选择“生成政策草案”。
2. 显示中央 Agent loading 和结构化生成过程，不使用假打字动画冒充流式推理。
3. 草案返回后显示模型策略标签、版本和 evidence refs。
4. 用户修改参数时实时显示 diff 和校验。
5. 用户选择“批准并进入全国推演”。
6. 成功后路由到 `/experiments/:id/live`；失败时保持草案和编辑内容。

### 7.5 必须覆盖的异常

- Agent Schema 第一次失败并修复成功。
- 二次失败进入中央草案 fallback。
- 组合权重错误。
- 审批 API 403/409。
- 页面刷新后恢复 draft 或 awaiting approval。

---

## 8. `/experiments/:id/live`：31 省政策决策与企业反馈

对应 Stitch `_2`。该页是产品主工作台，必须在 1440 × 900 首屏提供地图、核心指标、阶段和中央研判入口。

### 8.1 布局

- 顶部：实验元数据和 T0–T5 阶段条。
- 左侧 3 列：政策摘要、当前阶段、省级姿态/目标/中央支持请求摘要。
- 中间 6 列：真实 31 省地图、图例、分支/指标切换。
- 右侧 3 列：省级决策优先的结构化行动流、预警、中央研判入口。
- 下方可滚动区：省级目标与省际策略分布、企业聚合、fallback 省份和机制贡献。

如果 1440 × 900 空间不足，优先保证：地图、当前阶段、六项指标入口、河南详情、预警和 T3 中央研判入口可达；低优先表格下移。

### 8.2 全国地图

- 默认显示当前阶段的地方执行强度。
- T1 尚未产生地方行动时显示“等待省级决策”的空值图层，不回退到企业参与或用 0 填充。
- 指标切换分为两组：省级策略组包含地方执行强度、SME 倾斜、地方配套和中央支持请求；企业/环境组包含全国六项结果指标。切换不触发模型调用。
- 图例显示指标名、`指数/100`、阶段、分支和数值区间。
- 省份 hover 显示名称、当前值、指数点变化、质量/fallback 标签。
- 点击省份进入 `/experiments/:id/provinces/:provinceCode`。
- 河南使用国家行政区划代码 `41` 作为验收深链；旧 `?province=41` 必须兼容导航到该路由。
- 缺数据省份使用明确 hatch/空值样式和文字，不涂成 0。

### 8.3 行动流与阶段推进

行动流必须呈现真实 SSE 事实：

- T0 决策画像就绪。
- T1 省级策略开始/完成/fallback，显示主要目标、决策姿态和省际策略。
- T2 企业批量决策开始/完成/fallback。
- T3 省级复盘、调整意向和中央支持请求。
- 企业聚合更新和环境反馈。
- 环境更新和机制贡献。
- T3 Checkpoint 冻结、中央建议生成和待审批。

主 CTA 根据状态变化：

- 运行下一阶段
- 查看 T3 中央研判
- 正在运行（禁用）
- 恢复连接
- 查看对照结果

不得让用户通过重复点击并发触发同一阶段。

### 8.4 企业聚合

企业聚合位于省级策略摘要之后，全国页不同时展开 186 张卡片。使用聚合视图：

- 四种参与状态分布。
- 三种更新类型和无升级分布。
- 五种融资选择分布。
- fallback 省份计数和清单。
- 可筛选某类企业群体查看全国分布。

点击聚合项可改变地图或排行筛选，但不更改权威 WorldState。

### 8.5 断线恢复

- SSE 中断时页面进入 `reconnecting`，保留最后完整 WorldState。
- 显示最后 `event_id` 和重试状态。
- 重连携带 `Last-Event-ID`，客户端去重。
- 恢复后重新获取 WorldState，不能只靠缺失事件推断完整状态。

---

## 9. `/experiments/:id/provinces/:provinceCode`：省级 Agent 详情

该页沿用 Stitch `_3` 的高密度卡片、三栏布局和克制视觉，但把信息主角从企业矩阵改为省级决策。页面必须能从 Live 地图、行动流、Intervention 省级证据和 Compare 地区排行进入。

### 9.1 页面结构

信息顺序固定为：

1. 本次实验决策画像。
2. 优先目标与关键约束。
3. 当前地方决策。
4. 省际策略。
5. T3 调整意向与中央支持请求。
6. T1/T4 行动时间线。
7. 六类企业反馈证据。
8. 环境机制与结果。

1440 × 900 首屏必须可见主类型、六轴入口、当前目标、决策姿态、核心地方工具、省际策略和行动时间线入口。企业卡可在首屏下缘开始，但不能取代省级决策区。

### 9.2 决策画像、目标与约束

- 省份名称、行政区划代码、当前阶段/分支、fallback 和 evidence 入口。
- 主/辅助类型，例如“普惠扩散型”；固定显示“本次实验决策画像”与 `proxy/demo`。
- 六项百分位决策轴，提供图形和文本值，不显示伪置信度。
- 1–3 个优先目标、两项关键约束及其 Profile/Network/method version。
- 明确说明画像服务于当前实验，不代表现实省级政府立场。

### 9.3 地方决策与省际策略

- 主要目标、主动/平衡/审慎决策姿态和 1–3 个目标企业群体。
- 地方执行强度、配套比例、工具组合、SME 倾斜、区域投放、技术组合和中央支持请求。
- 合作联动、对标跟进、竞争争取或独立推进，以及 0–2 个 Top-K 目标省份。
- 原因码、最多 80 字公开摘要、模型/Cache/Fallback 标签和上一行动引用。

### 9.4 T3 调整意向与行动时间线

- 策略评价、重点企业信号、主要约束、中央支持类型与强度。
- 最多三项 `path + increase/decrease/hold + reason` 调整意向，并标记“尚未应用”。
- T1、原始方案 T4、干预方案 T4 按行动 ID 串联；不存在的未来行动显示前置状态，不使用伪数据。
- Persona 在所有节点保持一致；主动 Policy 差异和响应性 Action 变化使用不同标签。

### 9.5 企业反馈与机制

六类企业必须完整、顺序稳定，显示参与、更新类型、融资选择、投入强度、支持请求、原因码和模型/fallback 标签。企业的 `wait` 翻译为“观望”，不得显示成省级立场。

机制贡献展示政策匹配、直接补贴、贷款贴息、融资担保、SME 倾斜、区域支持、融资约束和财政成本，并使用环境计算标签。Compare 进入时支持原始方案/干预方案切换以及省级和企业迁移对照。

---

## 10. `/experiments/:id/intervention`：T3 干预审批

对应 Stitch `_4`。该页采用严格三栏结构，是产品审批可信度的核心页面。

### 10.1 三栏结构

#### 左栏：证据

- T3 Checkpoint 摘要和父状态版本。
- 省级主要目标与决策姿态分布。
- 中央支持请求、T3 调整意向和省际策略聚类。
- 重点省份的 Persona、地方工具和行动谱系入口。
- 六项全国指标及异常方向。
- 企业参与/融资/更新类型分布作为省级策略的市场反馈证据。
- fallback 范围、数据质量和证据引用。
- 每条证据可打开 `?evidence=...`。

#### 中栏：AI 建议

- 中央政策研判 Agent 的建议摘要。
- 最多 3 个选项；时间紧张时只展示 1 个完整选项。
- 每个选项包含结构化参数 diff、目标指标、预期方向、可能代价、证据引用。
- 预期方向统一显示“待验证”，不能写成已经发生的结果。
- 不使用“最佳方案”“必然改善”或“系统推荐最优政策”。

#### 右栏：人类审批

- 完整 diff、受影响阶段和将创建的分支说明。
- 批准、修改、拒绝三种操作。
- 修改时只允许编辑 Schema 允许字段，并重新校验权重。
- 审批人、时间、修改 diff 和备注进入 Replay。
- 明确提示：只有批准后才能从当前 T3 Checkpoint 创建 Treatment。

### 10.2 状态与门禁

- 没有 T3 Checkpoint：显示前置条件，不渲染伪证据。
- 建议生成中：保留证据栏，AI 栏显示 loading，审批栏禁用。
- fallback 建议：显示原因，仍允许用户审核结构化内容。
- 已拒绝：不创建 Treatment，可返回 Live 继续原始方案。
- 已修改并批准：保留 Agent 原建议和用户最终版本的 diff。
- API 返回未审批/非法状态时展示稳定错误码。

### 10.3 首屏要求

1440 × 900 下，三栏标题、关键证据、主要建议摘要和三个审批动作必须可达。长证据在栏内滚动，不得把审批按钮推到页面不可见区域。

---

## 11. `/experiments/:id/compare`：A/B 对照与中央复盘

对应 Stitch `a_b`。中性命名固定为：

- Control：原始方案
- Treatment：干预方案

在结果完成前不得称“优化方案”。

### 11.1 布局

- 顶部：同源性证明，显示父 T3 Checkpoint、政策 diff、版本和 seed 规则。
- 第一行：原始方案/干预方案地方执行强度双地图，共用指标、图例和色阶。
- 第二行：省级主要目标、决策姿态、地方工具、目标企业和省际策略迁移。
- 第三行：六项全国指标对照、差值和方向。
- 第四行：企业动作迁移矩阵、重点企业群体变化、地区排行。
- 第五行：机制归因、代价、fallback 说明和中央复盘。
- 证据入口始终可见。

### 11.2 双地图

- 两张地图必须真实渲染，禁止空白占位、截图或复用同一张静态图。
- 使用同一 GeoJSON/SVG、指标、色阶和区间，避免视觉误导。
- hover 和点击同步省份；点击打开 province drawer。
- 默认地方执行强度；可在省级策略组和企业/环境结果组之间切换，但两图必须同步切换。
- 未运行完成的分支显示状态层，不使用 0 值伪装结果。

### 11.3 指标对照

- 原始值、干预值、指数点差。
- 对财政压力和区域差距明确下降方向。
- 显示“环境计算”来源。
- 显著性、概率或置信区间只有在真实定义和计算存在时才能展示；P0 默认不显示。

### 11.4 省级策略迁移

- 对 31 个省分别比较主要目标、决策姿态、地方工具、目标企业群体和省际策略。
- 展示上一行动、原始方案 T4、干预方案 T4 和唯一主动 Policy Diff。
- 迁移必须来自 `comparison-v3`，不得由前端自行推断或用中央 Agent 文案替代。
- 点击任一省进入省级 Agent 详情页并保留当前分支上下文。

### 11.5 企业动作迁移矩阵

企业动作迁移位于省级策略迁移之后。

至少支持 `participation` 的 4 × 4 迁移：

- 参与
- 条件参与
- 观望
- 拒绝

矩阵单元显示企业群体数量，并可展开到省份和企业类型。还应提供：

- 更新类型迁移摘要。
- 融资选择迁移摘要。
- 重点企业群体变化卡。
- 变化来源于政策主动差异、环境传播或 fallback 的标记。

### 11.6 地区排行与机制归因

- 显示收益、受损、财政压力和区域差距变化，不只展示全国总分。
- 排行统一使用指数点，不使用现实增长率。
- 机制归因来自环境计算过程，不让中央 Agent 事后猜测。
- 每条机制可打开 evidence drawer 查看公式版本、输入和贡献守恒信息。

### 11.7 中央复盘

中央 Agent 的 T5 `CentralReview` 必须：

- 明确引用结构化 A/B 结果和证据。
- 区分已观察的环境计算结果与建议性解释。
- 说明改善、代价、地区损失和不确定性。
- 不生成现实预测、最终政策结论或“最优方案”。

---

## 12. `?evidence=...`：方法与证据抽屉

证据抽屉在五个路由中可打开，建议宽度 480–560px。

### 12.1 必须展示

- 证据 ID、类型、标题和简述。
- 数据质量：`verified` / `proxy` / `demo`。
- 来源名称、链接、统计年份、单位。
- 转换/标准化方法和缺失处理。
- `data_version`、`mechanism_version`、`prompt_version`、`model_version`、`app_version`。
- seed 和随机性规则。
- Experiment、Branch、Checkpoint ID 和父 T3 Checkpoint。
- Agent 输出证据引用或环境贡献明细。
- fallback 原因、范围和发生时间。
- 地图资源的来源、版本、适用范围、审图号或合规说明。

### 12.2 交互

- 证据引用使用真实按钮/链接，不用只有下划线的静态文字。
- 外部链接明确打开新窗口并标识域名。
- 可复制证据 ID 和版本信息。
- 缺少来源时显示“来源未完成核验”，不得编造链接。
- Evidence API 失败时保留错误码和重试操作。

---

## 13. 地图资源与合规门禁

### 13.1 技术实现

- 使用 ECharts `registerMap` 注册项目内离线 SVG，全国页和 A/B 页共用同一注册结果、指标定义和色阶。
- 正式资源存放在项目内，构建时打包，不在运行时远程抓取。
- 省级 feature 必须包含稳定行政区划代码和中文名称。
- 仿真显示范围严格为中国大陆 31 个省级行政区，港澳台范围说明放在方法页。
- 地图数据与 31 省 DTO 建立自动完整性检查。

### 13.2 来源记录

地图资源需在 provenance 文件和 evidence drawer 中记录：

- 来源机构和原始页面。
- 下载日期和资源版本。
- 适用范围和授权/使用条款。
- 是否直接使用标准地图。
- 审图号；如资源或修改场景不适用审图号，记录明确合规说明。
- 几何是否修改、简化和转换，以及处理脚本版本。

当前构建资源来自自然资源部标准地图服务 GS(2016)1609。原始 EPS、SHA256、转换脚本、31 省标注、省域几何签名与比赛发布决定记录在 `apps/web/src/assets/maps/README.md`。根据已确认的比赛规则，比赛版无额外地图合规审核门禁，可直接上线。

### 13.3 阻断条件

存在以下任一情况时，地图任务不得标记完成：

- 来源或授权不明。
- 缺省、重复省份或行政区划代码不一致。
- 用截图、CSS 块状图、自绘抽象 SVG 或网络底图替代。
- A/B 两图使用不同几何或色阶。
- 未说明港澳台和仿真范围差异。

---

## 14. React 实现约束

### 14.1 应用结构

- 使用正式 React Router 管理路由和 query drawer。
- 页面从统一 API client 和 hooks 获取 DTO。
- SSE 连接由专门 hook 管理，提供去重、重连和状态刷新。
- Server State 与抽屉、筛选、展开等局部 UI State 分离。
- 不在 React 组件内重算权威实验结果。
- 不把 31 省 WorldState 复制到多个互相不同步的 store。

### 14.2 Stitch 代码使用边界

可以参考 `code.html`：

- 栅格比例
- 卡片密度
- 间距和排版层级
- 静态组件的视觉尺寸

不得复用：

- 静态 HTML 页面作为 iframe。
- 内联模拟数据冒充 API。
- `href="#"`、onclick 占位和假路由。
- CDN Tailwind、字体、图标或地图。
- 静态截图地图和空白占位区。
- 错误品牌、官方暗示和伪精确文案。

### 14.3 本地资源

- 字体、图标、地图和必要图片均纳入版本控制或受控构建资产。
- 记录来源、许可证和版本。
- 不为视觉装饰引入与产品无关的大图和生成式背景。
- 加载失败时显示功能性 fallback，不影响关键审批操作。

### 14.4 性能

- 首屏避免一次渲染 186 张企业卡。
- 地图、图表和重列表按页面需要懒加载。
- SSE 更新批处理，避免每个企业事件导致全页面重绘。
- 1440 × 900 和 1280 宽下不得出现水平页面滚动；表格可使用受控内部滚动。

---

## 15. 可访问性与语言

- 全站中文为主，只保留 T0–T5、Agent、Control/Treatment、SME、ID 等必要机器概念。
- 所有图标按钮有可访问名称和 tooltip。
- 输入框有关联 label、帮助和错误信息。
- 键盘可完成创建、地图省份进入、抽屉、审批和 A/B 指标切换。
- 焦点样式清晰，不被 `outline: none` 移除。
- 文本和背景满足至少 WCAG AA 对比度目标。
- 图表提供文本摘要和表格替代，不仅依赖颜色。
- 动效遵守 `prefers-reduced-motion`，非必要动效可关闭。
- 状态、风险和分支不能只用颜色区分。

---

## 16. 文案底线

### 16.1 必须使用

- “研判口径：结果为当前数据与机制参数下的模拟指数”
- “模拟指数变化”
- “待验证”
- “原始方案 / 干预方案”
- “智能体策略 / 机制测算 / 审批决策”
- “已核验 / 代理数据 / 演示数据”，审计层保留 `verified / proxy / demo` 机器码
- “规则接管：本省企业响应由确定性规则生成”
- “本次实验决策画像”
- “省级 Agent 在当前实验中选择”
- “调整意向尚未应用”

全站默认使用中文业务操作文案：标题说明当前工作，状态说明当前进度，按钮说明即将执行的操作。不在主界面解释内部 Schema、接口、缓存键、Replay 或实现原理，相关信息收纳至数据与审计抽屉。

### 16.2 禁止使用

- “预计 GDP 将增长 X%”
- “新增就业 X 万人”
- “投资增加 X 亿元”
- “生产率提升 X%”
- “政策一定有效”
- “最优政策 / 最佳方案 / 优化方案”
- “国务院认为 / 某省政府决定 / 企业承诺”
- “省份性格 / 某省偏爱 / 某省天生倾向”
- “可信度 96% / AI 置信度 92%”
- `Official Institutional Use Only`

前端测试必须包含禁止文案和禁止单位扫描。

---

## 17. 视觉与交互 QA

### 17.1 截图矩阵

正式前端交付前，至少生成：

| 页面/状态 | 1440 × 900 | 1280 宽 |
|---|---:|---:|
| 新建政策 draft | 必须 | 必须 |
| 新建政策 awaiting approval | 必须 | 可选 |
| Live running | 必须 | 必须 |
| 河南省级 Agent 详情页 | 必须 | 必须 |
| 广东/山西决策画像 | 必须 | 可选 |
| T3 intervention awaiting approval | 必须 | 必须 |
| Compare completed | 必须 | 必须 |
| Evidence drawer | 必须 | 可选 |
| Fallback | 必须 | 可选 |
| Failed / reconnecting | 必须 | 可选 |

### 17.2 视觉比较方式

每个核心页面：

1. 使用相同 1440 × 900 viewport 截取 Stitch 源图和正式实现。
2. 将源图与实现图放入同一个比较输入或并排画布。
3. 检查布局、间距、字号、字重、边框、圆角、颜色、遮挡和首屏可达性。
4. 记录差异优先级并修复。
5. 重新截图和比较，直到所有 P0/P1/P2 关闭。

单独查看实现截图不算 Design QA。

### 17.3 `design-qa.md`

交付物必须包含 `design-qa.md`：

- 基准图和实现图路径。
- viewport、浏览器和构建版本。
- 页面状态和测试数据集。
- P0/P1/P2/P3 差异清单。
- 修复提交或文件引用。
- 地图来源/合规检查结果。
- 可访问性和禁止文案扫描结果。
- `final result: passed | failed`。

只有 `final result: passed` 才能把 Stitch 前端任务标记完成。

V2 的历史 `passed` 必须保留在独立章节；V2.1 在正式实现和截图比较前固定为 `pending implementation`。

---

## 18. 自动化验收清单

### 路由与门禁

- [ ] 五个路由可直接访问并正确恢复状态。
- [ ] 旧 `?province=` 正确导航到独立省级详情页。
- [ ] 未批准 T0 不能进入运行态。
- [ ] 未批准 T3 不能创建干预方案。
- [ ] 已完成阶段可回看，未来阶段不可越过。

### 地图与抽屉

- [ ] 31 省完整、唯一且可点击，默认显示地方执行强度。
- [ ] 河南详情先显示普惠扩散型画像、地方决策和省际策略，再显示六类企业反馈。
- [ ] 广东/山西分别显示技术跃迁型/绿色转型型实验画像。
- [ ] A/B 双地图都真实渲染并同步指标、图例和省份。
- [ ] `?evidence=...` 可深链、刷新和返回。
- [ ] 地图 provenance 和合规字段完整。

### 数据语义

- [ ] 六项全国指标名称、单位和方向正确。
- [ ] Persona 六轴明确为 31 省百分位，不显示伪置信度。
- [ ] 结果只使用指数和指数点变化。
- [ ] 政策参数可使用百分比，结果不得误用百分比。
- [ ] `verified` / `proxy` / `demo` 不显示伪置信度。
- [ ] 企业 `wait` 显示为企业“观望”。

### 审批和分支

- [ ] T0 可修改/批准，T3 可批准、修改或拒绝。
- [ ] T3 省级调整意向标记“尚未应用”且不修改政策。
- [ ] diff、审批版本和父 Checkpoint 可追溯。
- [ ] 原始方案/干预方案来自同一 T3 Checkpoint。
- [ ] API 403/409 和稳定错误码可见。

### 状态与可靠性

- [ ] loading、empty、running、awaiting approval、approved、completed 可见。
- [ ] fallback、failed、reconnecting 可恢复且不静默。
- [ ] SSE Last-Event-ID 和去重不产生重复卡片。
- [ ] 页面刷新不丢失已保存的审批和实验状态。

### 视觉和可访问性

- [ ] 1440 × 900 首屏核心入口可达且无遮挡。
- [ ] 1280 宽主流程可用且无页面级横向滚动。
- [ ] 键盘、焦点、对比度和图表文本替代通过检查。
- [ ] 无 CDN、无 iframe 静态页面、无 `href="#"`。
- [ ] 禁止品牌、官方暗示、现实预测和伪精确文案扫描通过。
- [ ] `design-qa.md` 的 V2.1 最终结果为 `passed`。

---

## 19. 前端完成定义

V2.1 Stitch 前端只有同时满足以下条件才完成：

- 五路由和证据抽屉接入正式 React 应用、真实 API 与 SSE。
- 31 省地图资源通过来源、省域绑定、几何签名和比赛上线门禁。
- 31 个 Persona、地方决策、省际策略和行动谱系使用真实 V3 DTO。
- 河南详情展示普惠扩散型实验画像和六类企业真实反馈，不使用硬编码结果。
- T0/T3 审批不能绕过，A/B 同源证据可见。
- 省级策略迁移优先，企业行为迁移、地区排行、机制归因和中央复盘完整。
- 所有状态、单位、来源标签和免责声明符合 PRD。
- 1440 × 900、1280 宽、核心 E2E、禁止文案和可访问性检查通过。
- `design-qa.md` 的 V2.1 `final result` 为 `passed`。

本完成定义尚未满足。V2 前端可继续作为已通过基线运行，但 V2.1 不得在新的领域、仿真和 API 契约实现前开始虚构页面数据，也不得沿用历史 Design QA 结论。
