# ADR-370：M35 Presentation 因果舞台与博弈展示层

状态：Approved
日期：2026-08-13
范围：`apps/presentation`、M34 Presentation 只读投影与专项验收

## 1. 决策

M35 以用户选定的“因果舞台”视觉稿为正式目标，原地重建 `apps/presentation`。普通 Web 前端不属于本里程碑的产品界面或验收范围。M34 的季度运行时、消息权限、交易状态机、季度环境、Checkpoint 与年度 Comparison 继续作为唯一业务真相；M35 不增加 Agent 调用，不修改季度结果，不从展示摘要反推权威指标。

正式体验由三层组成：

```text
Story：回答当前季度发生了什么，以及为什么重要
Explore：在地图与主体之间探索同一冻结事实
Evidence：查看方法、来源、fallback 与版本
```

每个有互动的季度节点必须形成可阅读的六段链：

```text
关注 FOCUS
  → 观察 OBSERVE
  → 决策 DECIDE
  → 行动 ACTION
  → 回应 RESPONSE
  → 结算 SETTLE
```

六段链只保存并展示结构化观察、行动、替代项、机会成本、改变条件、实际消息、交易状态和环境贡献，不展示模型思维链。

## 2. Presentation View Model

`presentation-frame-v4` 是运行时事实到用户界面的强制投影。前端主舞台不得直接读取 `InteractionMessage`、`InteractionSession`、`AgentTickDecision` 或 `WorldState` 的原始字段。

每帧至少包含：

- `question`：当前季度唯一关键问题。
- `chapter`：Q1 政策落地、Q2 主体互动、Q3 调整扩散或 Q4 年度结算。
- `branch_views`：Control 与 Treatment 完全隔离的地图、指标、互动和 Spotlight。
- `spotlight`：一个主互动及最多两个辅助互动，包含主体展示名、主体类型、目标、约束、观察、决策、行动、实际回应、取舍、改变条件、状态和结算贡献。
- `event_impacts`：事件到省份/车企的显式因果覆盖层。
- `display_metadata`：已映射的中文标签、单位和语义状态；不得把机器枚举当文案。

展示主体引用固定使用：

```text
province:<province_code>
automaker:<automaker_id>
event:<event_plan_id>
environment:national
```

后端负责把省份、车企、消息类型、交易状态、参与模式、季度节点和机制码映射为稳定业务语义。未知值只能进入 Evidence 的“未识别字段”诊断，不得直接显示在主舞台。

## 3. 旅程与视觉层级

主舞台固定为：

1. 顶部：品牌、当前决策问题、原始方案/干预方案/差值和事实状态。
2. 左侧：六段因果链，当前节拍高亮，其他节拍保持可扫读。
3. 中央：低饱和中国地图与当前 1–3 条关键关系；互动发生时热力图退为世界背景。
4. 右侧：当前主体博弈台，显示“为何行动、提出什么、如何回应、当前结果、何时改变”。
5. 底部：Q1–Q4 四章年度时间轴；季度内节点使用“首次行动 / 条件回应 / 协议收敛”等业务文案，不显示 `wave_0` 等工程名称。

地图主舞台提供“自动镜头 / 全国俯视 / 因果侧视”三种展示偏好。自动镜头只在“行动、回应”节拍切换为倾斜侧视，用于强调当前主体、行动路径和实际回应；关注、观察、决策、结算恢复全国俯视。差值、季度结算和年度比较强制使用全国俯视，确保省域色阶和同源 A/B 的空间判断不被透视扭曲。视角只改变前端相机，不创建业务帧、不修改权威结果。

首次观看者在 10 秒内必须能回答：当前季度、行动主体、行动原因、博弈对象、对方回应和对世界结果的影响。

## 4. 地图、色阶与分支

- 互动帧使用低饱和世界状态底图，关系线和主体成为第一视觉层。
- 结果帧使用语义色阶。绝对值提供低/中/高含义；差值固定显示负向、零点和正向语义。
- 同一指标跨季度使用同一年度域；同步 A/B 使用同一共享域。不得逐帧重标尺造成伪变化。
- 原始方案互动只读取 Control；干预方案只读取 Treatment；差值视图只显示后端明确标记的分歧，不混合两分支关系。
- 提议为琥珀实线，反报价为紫色虚线，达成为青色粗线，拒绝为红灰淡出；状态还必须有文字和线型，不能只靠颜色。
- 事件帧必须显示 `Event → Province/Automaker → Decision → Settlement`，不能只有时间轴菱形节点。
- 全国版图完整性是展示边界而非计算边界：31 省参与推演，港澳台与同一冻结标准地图中的南海诸岛附图保持可见但不进入指标、事件、Agent、交互或比较。

## 5. 动画

动画严格服从冻结事实，顺序为：

```text
FOCUS 镜头聚焦
→ OBSERVE 观察事实显现
→ DECIDE 决策与取舍显现
→ ACTION 提议路径绘制
→ RESPONSE 回应路径与状态变化
→ SETTLE 关系收敛、地图与指标更新
```

每段动画都可暂停、单步和低动效替代。动画不得插值业务决策或伪造不存在的中间结果。

WebGL 使用真实地图俯仰相机完成侧视；SVG 兼容渲染使用同构透视变换，并保持省域、主体、关系与点击语义。低动效取消长距离镜头飞行，仅保留短状态切换。

## 6. 字段泄漏门禁

主舞台 DOM 禁止出现：

- snake_case 字段名与原始枚举值；
- `wave_0|wave_1|wave_2`、`sender_id`、`recipient_ids`、`transaction_state`、`message.kind`；
- 裸省份代码、裸车企 ID、Checkpoint ID、WorldState、Schema 版本或哈希；
- 未映射的 fallback 原因和机制码。

Evidence 层可显示必要技术值，但必须同时提供中文字段名和上下文。自动化测试对主舞台执行 DOM 泄漏扫描。

## 7. 验收

- Projection 单测证明分支隔离、主体前缀、六段链、事件因果层和稳定 Spotlight。
- Presentation E2E 覆盖纯政策、政策压力和事件反事实三类实验。
- 互动帧能绘制省际、省企与事件关系；SVG 回退保持相同语义。
- 1920×1080、2560×1440、3840×2160 和回退画布完成视觉 QA。
- `design-qa.md` 以用户选定视觉稿为源真相，修复全部 P0/P1/P2 后才可 `passed`。
- API 与 Presentation 启动后进行首次观看者可理解性检查；只证明页面能运行不算完成。

## 8. 被拒绝方案

- 继续在 M34 旧面板中补 CSS：无法恢复被投影丢弃的决策链与分支语义。
- 前端直接读取互动市场并自行推理因果：会重新引入越权、串支、顺序依赖和字段泄漏。
- 让热力图继续作为所有帧的第一视觉层：无法让用户理解主体博弈。
- 为动画生成中间业务值：违反 M34 权威帧与确定性环境边界。
